meta {
  name: Login
  type: http
  seq: 8
}

post {
  url: {{serviceurl}}/api/login
  body: json
  auth: none
}

body:json {
  {
    "username": "tim",
    "password": "password"
  }
}

script:post-response {
  bru.setVar("token", res.getBody())
}

settings {
  encodeUrl: true
  timeout: 0
}
