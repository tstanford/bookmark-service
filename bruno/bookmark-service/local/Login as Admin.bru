meta {
  name: Login as Admin
  type: http
  seq: 10
}

post {
  url: {{serviceurl}}/api/login
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "admin123"
  }
}

script:post-response {
  bru.setVar("authToken", res.getBody()["authToken"])
  bru.setVar("refreshToken", res.getBody()["refreshToken"])
}

settings {
  encodeUrl: true
  timeout: 0
}
