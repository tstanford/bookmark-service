meta {
  name: Login
  type: http
  seq: 7
}

post {
  url: http://localhost:8080/login
  body: json
  auth: none
}

body:json {
  {
    "username": "tim2",
    "password": "password"
  }
}

script:post-response {
  bru.setVar("token", res.getBody())
}

settings {
  encodeUrl: true
  timeout: 0
}
