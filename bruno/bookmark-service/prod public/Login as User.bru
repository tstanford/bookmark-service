meta {
  name: Login as User
  type: http
  seq: 7
}

post {
  url: {{serviceurl}}/api/login
  body: json
  auth: none
}

body:json {
  {
    "username": "emily",
    "password": "emily!"
  }
}

script:post-response {
  bru.setVar("token", res.getBody())
}

settings {
  encodeUrl: true
  timeout: 0
}
